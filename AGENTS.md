# 仓库执行规则

## 强制规则

1. 所有包安装必须在 conda 环境 `fire` 中执行。
2. 所有代码执行必须在 conda 环境 `fire` 中执行。
3. 仓库内新增或修改的文档必须使用中文撰写；禁止在文档系统中混用英文正文，除非引用不可翻译的专有名词、命令、路径或代码标识。

## 执行说明

- 若当前 shell 不在 `fire` 环境，则不得直接运行 Python、pytest、脚本或服务启动命令。
- 推荐使用 `conda run -n fire ...` 显式执行，避免误用 `base` 或系统 Python。
- 若实施计划或实际执行表明当前任务依赖尚未安装，代理必须先显式向用户请求并报备待安装的依赖清单；仅在用户确认后，才可继续在 `fire` 环境自动安装，并将安装内容、安装时机与验证结果记录到 `docs/status.md`。
- 若执行结果仅在非 `fire` 环境得到验证，`docs/status.md` 中必须明确标记为“未完成环境合规验证”。
- 凡是新增或修改测试，必须先运行对应的单元/预编写测试，再基于真实上游产物（如 `data/normalized`、`data/structured`、`data/chunks` 或真实源文档）补做一轮真实验证。

## 状态更新规则

- `docs/status.md` 仍是本仓库唯一的项目状态源，但不要求每次任务结束或每次代码执行后自动更新。
- 只有在出现重要里程碑、阻塞点变化、用户明确要求、长期任务交接、需要保存验证结果、依赖安装结果、环境合规状态，或信息会影响后续上下文时，才更新 `docs/status.md`。
- 纯文档小改、只读分析、简单问答、无状态影响的局部检查，不需要自动写入 `docs/status.md`。

## 计划撰写规则

- 撰写任何类型的“执行计划”时，都必须显式说明本 Phase / Task 是否需要安装依赖。
- 若需要安装依赖，必须同时写明拟安装的依赖清单和安装环境；若不需要，也应明确写成“无需新增依赖，沿用现有环境”。

## 文档读取规则

- 新开聊天窗口时，Codex 在项目层会自动读取本文件；`docs/` 下的业务文档不会被自动当作项目指令加载。
- 因此，任何需要稳定继承的仓库级规则，都必须优先写在本文件，而不是只写在 `docs/` 目录中。
- 为降低 context rot，新会话必须按“先索引、再状态、后按需深入”的顺序读取项目文档，而不是一次性读完整个 `docs/`。

### 新会话标准读取顺序

1. 先读取 `docs/文档索引.md`，理解文档分层和每类文档的职责。
2. 再读取 `docs/status.md`，确认当前做到哪一步、在哪个环境验证过、当前阻塞点是什么。
3. 当任务涉及产品目标、功能范围、用户故事、本期边界或产品验收时，读取 `docs/plans/2026-04-11-fire-qa-system-2.0-prd.md`；PRD 是当前唯一产品标准。
4. 当任务涉及技术架构、目录设计、运行命令、建库链路、证据约束、错误处理、测试策略或前端工程组织时，读取 `README.md`；README 是当前技术标准入口。
5. 只有当任务需要追溯一期 RAG 历史背景、旧实现来源或旧计划上下文时，才读取 `docs/plans/2026-03-28-fire-law-rag-design.md` 或 `docs/plans/2026-03-28-fire-law-rag-implementation.md`。
6. 当任务涉及实现推进、任务拆分、执行步骤、验收命令时，再读取对应实施计划；消防问答系统 2.0 主线优先读取 `docs/plans/2026-04-11-fire-qa-system-2.0-implementation.md`。

### 触发条件

- 若用户的问题只与“当前进度、阻塞点、最近做了什么”有关，只读取 `docs/status.md` 即可。
- 若用户要求继续执行既定计划，必须读取 `docs/status.md` 和实施计划。
- 若用户要求调整产品目标、功能边界或验收标准，必须读取 `docs/status.md` 和 2.0 PRD，必要时再看实施计划评估影响。
- 若用户要求调整技术架构、目录边界、运行方式、测试策略或工程约束，必须读取 `docs/status.md` 和 README，必要时再看实施计划评估影响。
- 若用户要求追溯一期 RAG 的历史设计、旧任务来源或旧实现取舍，才读取一期 RAG 设计文档和旧实施计划。
- 若任务与当前项目无关，不得机械性加载全部项目文档，应只保留与当前问题直接相关的上下文。

## Git 规则

- 允许代理在当前任务范围内自主执行 `git add` 与 `git commit`，用于记录本次已完成且已验证的改动。
- 代理提交后必须尽快向用户同步本次提交范围、已完成验证结果与实际使用的提交说明，并明确未夹带无关改动。
- 未经用户单独要求，代理不得执行 `git push`、`git merge`、`git rebase`、`git reset`、`git checkout --` 或其他改写历史、影响远端或可能误伤无关改动的 Git 操作。
- 若工作区存在代理未产生的改动，代理必须避开这些改动；若无法安全避开，必须先向用户说明风险，再决定是否提交。
