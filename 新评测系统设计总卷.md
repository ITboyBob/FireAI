# 新评测系统设计总卷

> **卷册角色**：新评测系统范围与分卷导航的唯一入口
>
> **最后更新**：2026-09-11（BOB-74）
>
> **当前状态**：Under Review；BOB-56 受 BOB-55 阻塞

## 当前范围

本卷册只设计独立的 evaluation 阶段。目标系统读取按 `eval_set/` 正式规范独立生成的评测数据集和已经冻结的 `metrics.json`，运行当前被测 RAG，计算适用于各 case 的评测维度，聚合本次运行结果，并直接写出 `report.json` 与 `report.md`。

本阶段只产出 baseline，不判断质量是否通过。任何不属于上述数据准备、被测 RAG 执行、评分、聚合和报告写出的工作，均不进入本卷册。

旧 Word/W RAG 评测系统的 Dataset、Query、Metric、报告、Dashboard、Streamlit、Rich 和报告代码均不得复用。新系统只调用当前 RAG 作为被测对象，不改变其在线行为。

截至 2026-08-27，正式文件组织已确认采用“三类 JSONL + manifest”，但 `eval_set/` 尚未发布三类 case 的正式机器 schema。现有七项 Metric 仅适用于 Golden Set。BOB-74 已为 Adversarial Case 新增两项 Metric 的语义边界（红线违规率、风险识别能力），由分卷三 §7 独占登记；其稳定 ID、Judge 输入、判分细则、聚合方式与通过阈值确定后才能执行 Adversarial 评测。Edge 必须先定义额外 Metric 才能执行评测。因此本文描述的是目标架构，不构成“整体规范已经验收”的声明。审查状态、独立/依赖问题和解决顺序以[BOB-56 规范依赖审查](./docs/architecture_or_strategy/2026-08-27-evaluation-system-specification-dependency-review.md)为准。

## 文档及目录职责

| 卷册 | 文档 | 只负责什么 | 不负责什么 |
| --- | --- | --- | --- |
| 总卷（本文） | `新评测系统设计总卷.md` | 划定 evaluation 范围，列出七个维度，规定读取顺序和跨卷交接规则 | 不定义字段、公式、rubric、依赖版本或运行步骤 |
| BOB-56 审查 | [评测系统规范依赖审查](./docs/architecture_or_strategy/2026-08-27-evaluation-system-specification-dependency-review.md) | 判断整体规范能否验收；分类独立/依赖问题；给出候选方案、解决顺序和验收门禁 | 不替 BOB-55 冻结正式数据 schema，不实现 runner 或 Metric |
| `eval_set/` | [评测数据集入口](./eval_set/CODEMAP.md) | 当前保存三类数据的生成输入、流程与边界；BOB-55 后负责发布正式 schema | 不定义 Metric、运行编排、报告或 run 状态 |
| 分卷一 | [评测执行与报告架构](./docs/architecture_or_strategy/2026-07-23-evaluation-optimization-loop-architecture-design.md) | 读取评测数据集与 `metrics.json`，运行被测 RAG，保存逐 case 观测，调用评分与聚合，维护 run 状态，直接写出两份报告 | 不生成评测数据集，不定义评分公式、结果字段或错误条件 |
| 分卷三 | [Metric 与评分契约](./docs/architecture_or_strategy/2026-07-27-ragas-response-reference-rubric-judge-design.md) | 定义 `metrics.json`、七项 Metric、统一结果、聚合算法，并统一登记评测运行的稳定错误码、处理动作与未决边界；另按 BOB-74 登记两项 Adversarial 新增 Metric 的语义边界（分卷三 §7） | 不生成评测数据集，不安排 run 步骤，不决定报告目录或 run 状态 |

总卷、BOB-56 审查、`eval_set/`、分卷一和分卷三分别回答“当前做什么”“现在能否验收”“怎样准备评测数据”“怎样运行”“怎样算分”。每个问题只有一个入口给出具体契约，其他文档只链接到它。

## 候选评测维度

各维度的输入、算法、输出、错误和聚合规则均由[分卷三](./docs/architecture_or_strategy/2026-07-27-ragas-response-reference-rubric-judge-design.md)独占定义。总卷只列出当前候选集合和所属模块。BOB-56 审查已经确认：Golden Set 七项仅用于 Golden Set；Golden Set 同时作为 Adversarial Case 的生成种子。BOB-74 已确定 Adversarial 首期评测目标为安全处置，并新增两项 Metric 的语义边界（分卷三 §7）；其判分细则与聚合契约待定后才能执行评测。Edge 的额外 Metric 必须在其目标确定后另行定义。

| 模块 | 评测维度 | 运行时提供的观测 |
| --- | --- | --- |
| 检索 | Precision | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | Recall | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | Context Relevance | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | MAP@5 | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | MRR@5 | 最终融合后的有序 chunks 与冻结评测输入 |
| 生成 | Answer Relevancy | 冻结评测输入与被测 RAG 回答 |
| 生成 | Response—Reference Answer | 冻结评测输入与被测 RAG 回答 |
| 安全（Adversarial） | 红线违规率（gate，BOB-74） | 冻结评测输入（恶意 Query）与被测 RAG 回答 |
| 安全（Adversarial） | 风险识别能力（capability，BOB-74） | 冻结评测输入（恶意 Query）与被测 RAG 回答 |

分卷一只负责采集并传递这些观测，不解释怎样计算；`eval_set/` 在 BOB-55 完成后负责提供正式 Query、与目标一致的参考输出和 Golden Set 所需完整相关 chunk 集，不解释怎样评分；Adversarial 的三项系统期待行为与红线行为不进入数据集，统一由分卷三 §7 的 Rubric 承载。Adversarial 在两项新增 Metric 的判分细则与聚合契约确定前、Edge 在额外 Metric 定义完成前，均不得使用 Golden Set 的七项 Metric。

## 读取顺序

所有新评测系统任务先读本总卷，再按任务进入唯一负责的分卷：

1. 准备或检查评测数据时，读取 `eval_set/`。
2. 判断整体规范能否实施或验收时，先读取 BOB-56 审查。
3. 定义或实现 Metric、统一结果和聚合时，读取分卷三。
4. 定义或实现运行流程、run 状态和双报告时，读取分卷一。
5. 撰写端到端实施计划时，依次读取 BOB-56 审查、`eval_set/`、分卷三、分卷一，使计划先通过决策门禁并取得独立生成的正式评测数据集，再实现评分契约，最后接通执行与报告。

不得只读取一个分卷后补写其他分卷拥有的字段或规则。

## 跨卷交接规则

BOB-55 与 BOB-56 的待决策项通过后，完整数据流固定为：

```text
eval_set/ 独立生成评测数据集
  -> 分卷一读取评测数据集与 metrics.json
  -> preflight 建立标识仓库现存 chunks 的系统 snapshot，并校验 C_q 属于 G_q
  -> 分卷一运行被测 RAG 并保存逐 case 观测
  -> 分卷一对 Golden Set 调用分卷三定义的七项 Metric
  -> 分卷一在新增 Metric 契约冻结后评测 Adversarial / Edge
  -> 分卷一调用分卷三或后续专项定义的聚合算法
  -> 分卷一建立不可变 AggregatedEvaluationResult
  -> JSON writer 与 Markdown writer 分别直接写出报告
```

- `eval_set/` 在 BOB-55 完成后独占正式评测数据集的生成规则与字段边界；在此之前，现有生成素材不得冒充正式评分输入。
- 分卷三独占 Golden Set 七项 Metric 的 `metrics.json`、算法、统一结果、聚合算法，以及评测运行已确定错误码、处理动作与尚未定义错误边界；另按 BOB-74 独占登记两项 Adversarial 新增 Metric 的语义边界与 Rubric（分卷三 §7），其接入契约冻结后另行生效。其他文档只引用，不复制值域。
- 分卷一独占调用顺序、逐 case 观测、`run_status.json` 完整 schema、run 状态、报告目录和两种报告的写出流程；其他卷只引用该 schema。
- \(G_q\) 是仓库现存 chunk 集合，由本次 run 的系统 snapshot 标识；\(C_q\) 是正式评测 case 中人工确认的完整相关 chunk 集，仅对适用的检索 case 必填。最终字段名由 BOB-55 决定，业务语义不得退化为单个生成来源 chunk。
- preflight 首次读取 chunks 时建立不可变的 run 内 \(G_q\) snapshot，并机械校验 \(C_q \subseteq G_q\)。同一 snapshot 用于最终 `report.json.system_snapshot`；它不作为上游冻结输入，也不创建独立 `system_snapshot.json`。
- 所有需要检索输出的 Metric 只消费本次实际返回的最终融合后有序 chunks；生成端 Metric 不消费 chunks。
- `report.json` 与 `report.md` 读取同一个不可变 `AggregatedEvaluationResult`；Markdown writer 不读取 JSON，也不调用旧评测系统的展示或报告依赖。
- 任一单项 Metric 发生错误时，分卷一继续运行其余 Metric，最终把 run 标记为 `incomplete`，并仍然写出两份报告。`incomplete` 只适用于这一类“Metric 有错但聚合和双报告均成功”的 run；RAG、观测、Recorder、聚合、结果构建、writer 或发布基础设施失败均为 `failed`，且优先级固定为 `failed > incomplete > completed`。不得提供另一种运行分支。
- 下游文档只能引用上游产物，不得复制其 schema、公式、rubric 或错误字段表。

## 实现状态

| 范围 | 当前状态 |
| --- | --- |
| 总卷、BOB-56 审查、`eval_set/` 与两份分卷的职责边界 | 文档所有权已确认；正式文件组织已确认采用三类 JSONL + manifest，具体 schema 仍待 BOB-55；Adversarial 两项新增 Metric 语义边界已由 BOB-74 登记，判分细则与聚合契约待定 |
| BOB-56 整体规范审查 | 已完成现状审查；结论为 Under Review，受 BOB-55 阻塞 |
| 评测数据集独立生成 | `eval_set/` 已有生成素材；正式 schema 与正式 case 文件待 BOB-55 决策 |
| Golden Set 七项 Metric 的适用边界 | 已确认；七项仅评测 Golden Set，Golden Set 同时是 Adversarial 的生成种子 |
| Adversarial 两项新增 Metric（BOB-74） | 语义边界已登记于分卷三 §7；首期目标为安全处置。稳定 ID、Judge 输入、判分细则、聚合方式与通过阈值待后续确定，尚未进入 `metrics.json` |
| Edge 额外 Metric | 待定义；Edge 指标不得预先假定 |
| 被测系统快照 | 设计中；第一阶段写入 `report.json.system_snapshot`，尚未实现 |
| 七项 Metric、统一结果与聚合 | 设计中，尚未实现 |
| 评测执行、逐 case 观测与 run 状态 | 设计中，尚未实现 |
| `report.json` 与 `report.md` 直接写出 | 设计中，尚未实现 |
| 真实模型调用与新评测系统端到端运行 | 尚未验证 |

当前状态只能用于安排后续实施，不得把设计完成或局部依赖可用表述为代码已经实现。
