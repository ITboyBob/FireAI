# 新评测系统设计总卷

> **卷册角色**：新评测系统范围与分卷导航的唯一入口
>
> **最后更新**：2026-08-09

## 当前范围

本卷册只设计独立的 evaluation 阶段。系统读取脚本生成、校验并冻结的 `val_set.json` 和已经冻结的 `metrics.json`，运行当前被测 RAG，计算八个评测维度，聚合本次运行结果，并直接写出 `report.json` 与 `report.md`。

本阶段只产出 baseline，不判断质量是否通过。任何不属于上述数据准备、被测 RAG 执行、评分、聚合和报告写出的工作，均不进入本卷册。

旧 Word/W RAG 评测系统的 Dataset、Query、Metric、报告、Dashboard、Streamlit、Rich 和报告代码均不得复用。新系统只调用当前 RAG 作为被测对象，不改变其在线行为。

## 四份文档及职责

| 卷册 | 文档 | 只负责什么 | 不负责什么 |
| --- | --- | --- | --- |
| 总卷（本文） | `新评测系统设计总卷.md` | 划定 evaluation 范围，列出八个维度，规定读取顺序和跨卷交接规则 | 不定义字段、公式、rubric、依赖版本或运行步骤 |
| 分卷一 | [评测执行与报告架构](./docs/architecture_or_strategy/2026-07-23-evaluation-optimization-loop-architecture-design.md) | 读取冻结输入，运行被测 RAG，保存逐 case 观测，调用评分与聚合，维护 run 状态，直接写出两份报告 | 不生成 `val_set`，不定义评分公式、结果字段或错误条件 |
| 分卷二 | [val_set 生成与冻结](./docs/architecture_or_strategy/2026-07-27-ragas-val-set-query-generation-design.md) | 从当前仓库全部 chunks 通过纯脚本生成目标数量的 Query 与参考答案，校验后冻结并发布 `val_set.json` | 不运行被测 RAG，不计算分数，不写报告 |
| 分卷三 | [Metric 与评分契约](./docs/architecture_or_strategy/2026-07-27-ragas-response-reference-rubric-judge-design.md) | 定义 `metrics.json`、八项 Metric、统一结果、聚合算法，并统一登记稳定错误码、处理动作与未决边界 | 不生成 `val_set`，不安排 run 步骤，不决定报告目录或 run 状态 |

四份文档分别回答“当前做什么”“怎样运行”“怎样准备评测数据”“怎样算分”。每个问题只有一份文档给出具体契约，其他文档只链接到它。

## 八个评测维度

八个维度的输入、算法、输出、错误和聚合规则均由[分卷三](./docs/architecture_or_strategy/2026-07-27-ragas-response-reference-rubric-judge-design.md)独占定义。总卷只固定维度集合和所属模块。

| 模块 | 评测维度 | 运行时提供的观测 |
| --- | --- | --- |
| 检索 | Precision@5 | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | Recall@5 | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | Context Relevance | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | Accuracy | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | MAP@5 | 最终融合后的有序 chunks 与冻结评测输入 |
| 检索 | MRR@5 | 最终融合后的有序 chunks 与冻结评测输入 |
| 生成 | Answer Relevancy | 冻结评测输入与被测 RAG 回答 |
| 生成 | Response—Reference Answer | 冻结评测输入与被测 RAG 回答 |

分卷一只负责采集并传递这些观测，不解释怎样计算；分卷二只负责提供冻结 Query、参考答案和相关集合，不解释怎样评分。

## 读取顺序

所有新评测系统任务先读本总卷，再按任务进入唯一负责的分卷：

1. 准备或检查评测数据时，读取分卷二。
2. 定义或实现 Metric、统一结果和聚合时，读取分卷三。
3. 定义或实现运行流程、run 状态和双报告时，读取分卷一。
4. 撰写端到端实施计划时，依次读取分卷二、分卷三、分卷一，使计划先产生冻结输入，再实现评分契约，最后接通执行与报告。

不得只读取一个分卷后补写其他分卷拥有的字段或规则。

## 跨卷交接规则

完整数据流固定为：

```text
当前仓库全部 chunks
  -> 分卷二发布正式 val_set.json
  -> 分卷一读取 val_set.json 与 metrics.json
  -> 分卷一运行被测 RAG 并保存逐 case 观测
  -> 分卷一调用分卷三定义的八项 Metric
  -> 分卷一调用分卷三定义的聚合算法
  -> 分卷一建立不可变 AggregatedEvaluationResult
  -> JSON writer 与 Markdown writer 分别直接写出报告
```

- 分卷二独占 `val_set.json` 的字段、纯脚本生成、校验和冻结规则。
- 分卷三独占 `metrics.json`、八项算法、统一结果、聚合算法，以及四文档已确定错误码、处理动作与尚未定义错误边界。其他分卷只引用，不复制值域；当前稳定错误码总数为 42。
- 分卷一独占调用顺序、逐 case 观测、`run_status.json` 完整 schema、run 状态、报告目录和两种报告的写出流程；其他卷只引用该 schema。
- 默认把 Ragas scenario 的 `source_chunk_ids` 所对应 chunks 直接冻结为正式、完整 \(G_q\)。这是 Fire 评测协议的业务假设，不是 Ragas 自动证明的完整性；该假设可导致检索指标系统性偏差。
- `system_snapshot.json` 不进入第一阶段执行计划、preflight 或报告构建；只在评测数据集生成完成后进入后续阶段。其后续契约已固定为独立、机器可校验文件，顶层按 `retrieval_pipeline`（有序数组）、`chunking`、`indexed_documents` 三个业务域组织，详细字段与当前代码投影由分卷一定义。
- 所有需要检索输出的 Metric 只消费本次实际返回的最终融合后有序 chunks；生成端 Metric 不消费 chunks。
- `report.json` 与 `report.md` 读取同一个不可变 `AggregatedEvaluationResult`；Markdown writer 不读取 JSON，也不调用旧评测系统的展示或报告依赖。
- 任一单项 Metric 发生错误时，分卷一继续运行其余 Metric，最终把 run 标记为 `incomplete`，并仍然写出两份报告。`incomplete` 只适用于这一类“Metric 有错但聚合和双报告均成功”的 run；RAG、观测、Recorder、聚合、结果构建、writer 或发布基础设施失败均为 `failed`，且优先级固定为 `failed > incomplete > completed`。不得提供另一种运行分支。
- 下游文档只能引用上游产物，不得复制其 schema、公式、rubric 或错误字段表。

## 实现状态

| 范围 | 当前状态 |
| --- | --- |
| 四文档职责与 evaluation-only 边界 | 已确认，已完成本轮文档收敛 |
| `val_set.json` 纯脚本生成、校验与冻结 | 设计中，尚未完成真实数据发布验收 |
| `system_snapshot.json` | 后续契约已确定，仍延期至评测数据集生成完成后；不进入第一阶段 |
| 八项 Metric、统一结果与聚合 | 设计中，尚未实现 |
| 评测执行、逐 case 观测与 run 状态 | 设计中，尚未实现 |
| `report.json` 与 `report.md` 直接写出 | 设计中，尚未实现 |
| 真实模型调用与新评测系统端到端运行 | 尚未验证 |

当前状态只能用于安排后续实施，不得把设计完成或局部依赖可用表述为代码已经实现。
