# 消防智能接处警 MVP 系统设计方案 (Fire Dispatch MVP)

## 1. 系统定位与核心目标

本 MVP 系统的核心目标是**极速验证大语言模型（LLM）在消防接处警流程中的“信息抽取”与“指令生成”能力**。
为了避免被繁琐的非核心业务（如真实的资源查询、GIS测算、复杂的调度引擎）拖慢进度，本系统采用**“真实 LLM + 静态 Mock 假数据”**的组合策略。

同时，为了彻底解决原消防问答 RAG 系统（对话式 UI）与调度工作台（大屏 UI）在前端展现形态上的鸿沟，本 MVP 将在**全新的独立代码仓库**中孵化。

## 2. 核心架构设计

系统采用前后端分离的 Monorepo 结构，分为独立的 `frontend` 和 `backend` 两个子项目。

### 2.1 技术栈选择

*   **前端 (Frontend)**
    *   **核心框架**：Vite + React (TypeScript)
    *   **样式方案**：TailwindCSS
    *   **UI 愿景**：打造 Premium 级别的高级视觉体验。采用暗黑模式（Dark Mode）、毛玻璃特效（Glassmorphism）、微小交互动画（Micro-animations），营造出极具未来感与科技感的“调度指挥中心大屏”体验，确保极佳的演示效果（Wow Factor）。
*   **后端 (Backend)**
    *   **核心框架**：FastAPI (Python)
    *   **大模型调用**：LangChain 或直接使用官方 SDK（兼容现有的大模型 API 即可）
    *   **数据存储**：无需数据库。所有上下文数据（预案、周边消防站、车辆）均以 `.json` 静态文件格式存在。

### 2.2 核心业务流 (Pipeline)

系统采用极简的“两步走”流转机制：

1.  **用户输入**：在前端“接警输入框”输入一段模拟报警文本（例如：“XX小区着火了，烟很大”）。前端发起一次 `POST` 请求。
2.  **后端 Step 1 - 警情抽取 (LLM)**：调用 LLM 对报警文本进行信息抽取，输出结构化数据 `ParsedIncident`（灾种、地点、人员被困情况、缺失的关键信息）。
3.  **后端 Step 2 - 上下文融合 (Mock)**：后端加载一份写死的静态上下文配置 `mock_context.json`（模拟此时周边最近的是 A 消防站，有水罐车，可用水源正常等）。
4.  **后端 Step 3 - 指令生成 (LLM)**：将 Step 1 提取的结构化警情 + Step 2 的假上下文一并交给 LLM，LLM 负责润色并生成“规范、专业、可读”的最终调派指令与灾情提示（`CommandDraft`）。
5.  **前端展示**：后端将抽取结果和生成结果合并返回，前端通过骨架屏或打字机动画依次点亮“解析完成” -> “资源匹配完毕” -> “指令下发准备完毕”三个面板，展示完整的逻辑流转。

## 3. 接口设计草案

**API: `POST /api/v1/dispatch/simulate`**

**Request:**
```json
{
  "alarm_text": "喂，我们小区3号楼着火了，在12层，烟很大，可能有人没跑出来！"
}
```

**Response:**
```json
{
  "parsed_incident": {
    "disaster_type": "火灾",
    "location": "3号楼12层",
    "phenomenon": "烟很大",
    "casualty_risk": "可能有人被困",
    "missing_info": ["具体小区名称", "是否看到明火"],
    "follow_up_questions": ["请问您所在的具体小区叫什么名字？", "请问您有看到明火吗？"]
  },
  "mock_context_applied": {
    "closest_station": "A消防救援站",
    "available_vehicles": ["水罐车", "抢险救援车"],
    "rule_applied": "高层建筑火灾预案"
  },
  "command_draft": "【调派指令】命令A消防救援站出动水罐车、抢险救援车赶赴现场...\n【途中提示】现场为12层，烟大且可能有人员被困，请做好搜救准备..."
}
```

## 4. 后续演进路线

1.  **Phase 1 (本 MVP)**：完全打通前端大屏展示 + 后端 LLM 抽取 + Mock 资源融合生成指令的全链路。
2.  **Phase 2**：引入私域 RAG，将 `mock_context.json` 替换为真实的向量数据库检索，为调派指令提供真实依据。
3.  **Phase 3**：引入轻量级规则引擎（或图数据库查询），接入真实的车辆和消防站坐标，替换假资源调度。
