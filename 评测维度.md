# 旧评测指标

| 指标         | 执行方    | LLM 输入 / 规则内容                                          | 目前缺陷/疑问                                                |
| ------------ | --------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 上下文相关性 | LLM Judge | 输入：问题 + 每个实际检索片段的 `chunk_id`、前 500 字 `text`。Judge 对每个片段输出 `helpful=true/false`；程序计算“有帮助片段数 ÷ 检索片段总数”。不看答案。 | 本维度实际在分析“证据和问题的相关性”，即旨在评估系统的向量+关键词检索。 |
| 关键条文覆盖 | 规则程序  | 输入：题集中的 `expected_article` 与实际检索片段的 `article_no`。只要检索结果中存在完全相同的条号，得 `1.0`；否则 `0.0`。若 `expected_article=null`，直接记 `1.0`。 | 本维度同样旨在评估系统的向量+关键词检索，但属于兜底作用的“硬指标” |
| 忠实度       | LLM Judge | 输入：问题、系统答案、程序切出的断言列表、每个检索片段的 `chunk_id` 和前 500 字 `text`。Judge 对每个断言输出 `supported`、`not_supported` 或 `unknown`；程序计算“`supported` 数 ÷ 断言总数”。 | 1. 提示词仅要求“只看断言与证据的对应关系”。这形成了不必要的歧义<br />2. 忠诚度是“答案对证据的忠诚度”，和问题无关 |
| 答案相关性   | LLM Judge | 输入：问题 + 系统答案。Judge 打 1–5 分，程序除以 5 转为 `0–1`。不看法规证据，因此它只测“是否答题”，不负责判断法条真实性。 | 本RAG系统回答时答案直接来源于“检索的证据”，因此本维度实质在评估“问题和证据的匹配程度”，和维度1、2重复 |
| 引文有效性   | 规则程序  | 可回答题没有引文则得 `0`；否则每条引文先按 `matched_chunk_id` 找证据，再按上表比较 `path` 和 `text`。有效率 = 有效引文数 ÷ 引文总数。正确拒答的不可回答题不要求引文，记 `1.0`。 | 根据系统代码，有效引文指RAG系统最终返回的引文/证据，而“引文总数”实际指RAG系统检索到的top^k证据。在程序代码不存在bug、系统没有被恶意篡改的情况下，有效引文数始终等于引文总数 |
| 拒答适当性   | 规则程序  | 读取题集的 `answerable`、`expected_behavior` 和系统的 `refused`。可回答题却拒答，或不可回答题却作答，均记 `0` 并触发红线；其余记 `1.0`。`uncertainty` 虽被传入，但当前尚未检查拒答理由的质量。 | 暂时无                                                       |
