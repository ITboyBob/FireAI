# 消防法律 RAG 基础 Pipeline 与能力边界

## 一、基础 pipeline（现阶段可落地版）

| 顺序 | 环节 | 归类 | 你在“消防法律文本”场景里要做什么 | 产物 / 输出 | 现阶段是否必须 |
|---|---|---|---|---|---|
| 1 | 语料收集与范围定义 | 检索器 | 明确纳入哪些文本：如《消防法》、实施条例、地方消防条例、部门规章、规范性文件、执法指引等；同时决定是否只做消防法律规范文本，暂不纳入案例、问答、新闻 | 一份语料清单 | 必须 |
| 2 | 文档格式标准化 | 检索器 | 先把 `.doc` 尽量统一转成 `.docx`；去掉页眉页脚、页码、目录噪声、重复空行；保留原文结构 | 干净的标准化文本 | 必须 |
| 3 | 文档结构解析 | 检索器 | 从文本里抽出法律结构：法规名 / 章节 / 条 / 款 / 项；同时抽元数据：发布机关、发布时间、生效时间、失效时间、地域、效力层级 | 结构化文档（推荐 JSON） | 必须 |
| 4 | 切块（chunking） | 检索器 | 建议以“条”为主切块；如果单条太长，再按段落细分；但必须保留“父级路径”，例如“消防法 > 第三章 > 第28条” | 可检索的 chunk 集合 | 必须 |
| 5 | 建立检索索引 | 检索器 | 至少建一套向量索引；更稳妥的是再加一套关键词索引（BM25 一类）。法律文本里专有词、条号、机构名很重要，纯向量检索容易漏 | 向量库 + 关键词索引 | 建议必须 |
| 6 | 查询预处理 | 中间流程 | 对用户问题做轻量规范化：如“消防法”“中华人民共和国消防法”统一；识别问题里是否带地域、时间、处罚、职责、审批等线索 | 标准化查询 | 必须 |
| 7 | 检索执行 | 中间流程 | 用标准化后的问题去召回 chunk；优先按元数据过滤（地域/时间/文种），再做相似度检索；返回 top-k 结果 | 候选证据片段 | 必须 |
| 8 | 检索结果整理 | 中间流程 | 去重、按相关性排序、控制总 token；把每个 chunk 的来源信息一并带上：法规名、条号、章节路径 | 可直接喂给 LLM 的上下文 | 必须 |
| 9 | 答案生成 | 生成器 | 让 LLM 只基于检索结果回答；输出时要求它引用来源，例如“依据《消防法》第XX条”；若证据不足，要明确说“不足以回答” | 带出处的回答 | 必须 |
| 10 | 输出格式约束 | 生成器 | 建议固定输出模板：结论 / 依据条文 / 适用范围 / 不确定点 | 更稳定的回答格式 | 建议必须 |

---

## 二、这条基础 pipeline 目前的能力边界

| 能力项 | 目前大概率能做到 | 目前边界 / 风险在哪里 | 为什么会这样 |
|---|---|---|---|
| 条文定位 | 较强 | 基本没问题 | 语料是规范文本，结构清晰，最适合做检索 |
| 定义类问答 | 较强 | 基本没问题 | 如“什么是消防责任”“某主体职责是什么”这类问题，很适合条文召回 |
| 职责/处罚依据查询 | 较强 | 若多个文件并存，可能漏最合适那一条 | 这类问题通常能直接落到条文 |
| 简单流程梳理 | 中等偏强 | 若流程分散在多个文件中，可能回答不完整 | 基础 pipeline 没有多跳推理和复杂重排 |
| 多文件对照 | 中等 | 能做基础对照，但不一定能自动识别冲突、优先级、新旧版本替代关系 | 这已经接近法律解释层，而不只是检索层 |
| 时间效力判断 | 中等 | 强依赖元数据是否完整；没有版本治理就容易出错 | 法律文本不是“有内容就行”，还要知道是否仍然有效 |
| 地域适用判断 | 中等 | 若地方性法规混进全国法规但没标清地域，会误答 | 法律文本天然有地域边界 |
| 复杂法律推理 | 偏弱 | 不能稳健处理多个条文联动推导、例外情形、隐含限制 | 现阶段没有做复杂增强和验证 |
| 真实案件适用判断 | 偏弱 | 不能可靠回答“某现实行为是否合法、会不会被处罚” | 这不只是找条文，还涉及事实认定与法律适用 |
| 缺失语料场景 | 偏弱 | 语料库里没有的内容，LLM 可能答不出，或者乱补 | 基础 RAG 的能力上限受语料边界严格限制 |
| 解析鲁棒性 | 中等 | 如果 `.doc` 转换脏、条号识别错，后面全链路都会受影响 | 法律 RAG 的第一道坎往往不是模型，而是文本工程 |

---

## 三、你暂不做，但应单独留档的技术清单

### 3.1 训练方式技术清单

| 技术 | 主要归属 | 作用 | 什么时候值得接入 |
|---|---|---|---|
| 领域嵌入模型替换/微调 | 检索器 | 让“消防法律术语”的向量表示更准 | 基础检索召回率明显不够时 |
| Retriever 对比学习微调 | 检索器 | 让“问题—相关条文”更容易匹配 | 积累了问答对或标注数据后 |
| Reranker 训练 | 中间流程 | 提升 top-k 排序质量 | 召回到了，但最相关条文总排不靠前时 |
| LLM 指令微调 / LoRA | 生成器 | 让回答格式、引用风格、法律表述更稳定 | 基础生成经常跑偏或不守格式时 |
| 联合优化（检索+生成） | 全局 | 提升整条链路协同效果 | 项目成熟后再考虑 |

### 3.2 增强机制技术清单

| 技术 | 主要归属 | 作用 | 什么时候值得接入 |
|---|---|---|---|
| Query Rewrite | 中间流程 | 把用户模糊问题改写成更适合检索的问题 | 用户提问口语化、召回不稳定时 |
| Multi-query | 中间流程 | 同一个问题生成多个检索视角 | 一个问题经常涉及多个法条时 |
| Reranking | 中间流程 | 对检索结果二次排序 | 基础召回后结果顺序不理想时 |
| Metadata-aware Retrieval | 中间流程 | 更强地利用地域、时间、文种过滤 | 语料开始变多、版本开始混杂时 |
| Knowledge Graph / 条文关系图 | 中间流程 | 建立“法条—概念—职责—处罚”关系 | 要做复杂联动问答时 |
| Citation Grounding | 生成器/中间流程 | 强制答案和条文逐段对齐 | 开始追求高可解释性时 |
| Answer Verification | 中间流程 | 让系统先答再自检 | 开始担心“看起来对、实际没依据”时 |
| 时间版本控制 | 检索器/中间流程 | 解决新旧法、地方与全国版本混用 | 这是法律场景中后期几乎一定会需要的能力 |

---

## 四、这条 pipeline 的“能力上限”可以怎么理解

| 维度 | 当前基础版的上限 |
|---|---|
| 最适合做的事 | 条文检索、法条问答、出处引用、简单归纳 |
| 勉强能做的事 | 跨几份文件的基础整合 |
| 暂时不适合做的事 | 复杂法律解释、冲突裁判、真实执法/案件结论预测、自动给出非常确定的合规建议 |
| 当前最该投入的点 | 文档清洗、结构解析、条级切块、元数据治理、检索质量 |
| 最可能先出问题的点 | 不是生成器，而是检索器前面的文本处理和版本治理 |

---

## 五、一句话总结

现阶段最合理的路径，不是先做训练和复杂增强，而是先把 **文本标准化 → 条级结构解析 → 元数据治理 → 基础双路检索 → 受约束生成** 这条链路做稳。它会很适合做法条检索型问答，但边界也很清楚：**还不适合承担复杂法律解释和高风险结论判断。**

