---
Description:本项目创建的任何issue都必须在Linear的Project“消防AI”下创建
---



@AGENTS.md

# Claude Code 项目规则

## Claude Code 专属规则

### 1. 计划模式（Plan Mode）

- 计划必须获得用户明确批准后，方可进入执行阶段。

### 2. 子智能体（Subagents）使用规范

- 主线程发送给子智能体的 Prompt 必须任务边界清晰，属于可独立完成的 bounded work。
- 必须明确任务目标、范围、预期输出、完成条件。
- **禁止**把模糊的大任务直接丢给子智能体，再由主线程中途接管并重复劳动。
- 当子智能体迟迟不返回结果时，默认不要立即判定其"卡住"。应先查看子智能体线程是否仍在输出结果、是否在等待审批、工具调用或长任务完成；在未明确失败前，优先等待所有已派发子智能体返回结果后再统一汇总。只有在确认子智能体失败、阻塞或结果明显不满足要求时，主线程才接管。

### 3. 工具调用与权限

- 所有网页搜索、网页抓取、站点映射、内容提取、联网研究等动作，**必须**使用 Firecrawl、Exa 或 Tavily 这三款 MCP 中的一种；禁止改用其他通用网页搜索/抓取工具。
- 页面交互自动化（如点击、登录、表单填写、浏览器调试）可按需使用专门的交互工具（如 Playwright）。

### 4. 测试与验证

- 新增或修改测试后，必须先运行对应的单元/预编写测试。
- 再基于真实上游产物（如 `data/normalized`、`data/structured`、`data/chunks` 或真实源文档）补做一轮真实验证。

### 5. 文档读取顺序和规则

- 新会话先遵循根目录 `AGENTS.md`，再读取 `docs/system_meta/文档索引.md` 和 `docs/system_meta/文档读取规则.md`。后续只按任务映射加载文档，不得默认扫描整个 `docs/`。

- Claude Code 专属规则只能补充工具行为；若与 `AGENTS.md` 或文档读取规则冲突，以 `AGENTS.md` 为准。
- `docs/` 下的 Markdown 文档不得超过 500 行；接近上限420行时按职责拆分为总览与分卷。

### 6. 沟通风格

- 与用户沟通时保持简洁、清晰，避免过度技术化。
- 用户是大一新生，编程基础薄弱，解释概念时应耐心、循序渐进。
- 当用户想法存在技术风险或不可行时，应大胆质疑并给出替代方案。

### 7. 环境合规检查清单

每次执行代码前，快速确认：

- [ ] 当前是否在 `fire` conda 环境中？（推荐 `conda run -n fire ...`）
- [ ] 是否需要安装新依赖？（如需，先向用户请求确认）
